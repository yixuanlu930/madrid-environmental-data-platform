{
  "name": "Madrid 3 - Analitica",
  "nodes": [
    {
      "parameters": {
        "rule": { "interval": [{ "triggerAtHour": 8, "triggerAtMinute": 0 }] }
      },
      "id": "ScheduleTrigger",
      "name": "Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [-352, 96]
    },
    {
      "parameters": {},
      "id": "ManualTrigger",
      "name": "When clicking 'Execute workflow'",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [-352, 352]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const now = new Date();\nconst madridDate = new Intl.DateTimeFormat('sv-SE', { timeZone: 'Europe/Madrid' }).format(now);\nconst yesterday = new Date(madridDate);\nyesterday.setDate(yesterday.getDate() - 1);\nconst date = yesterday.toISOString().split('T')[0];\nconst parts = date.split('-');\nconst year = parts[0];\nconst month = parts[1];\nconst day = parts[2];\n\n// Genera 24 items, uno por hora, con la URL de descarga de cada fichero\nconst items = [];\nfor (let i = 0; i < 24; i++) {\n  const hourStr = String(i).padStart(2, '0');\n  const dateCompact = date.replace(/-/g, '');\n  const fileName = hourStr + '_' + dateCompact + '.csv';\n  const url = 'http://minio:9000/madrid-openmeteo-environment/processed/environment_observations_long/' + year + '/' + month + '/' + day + '/' + fileName;\n  items.push({\n    json: { date: date, year: year, month: month, day: day, hour: hourStr, fileName: fileName, url: url }\n  });\n}\nreturn items;"
      },
      "id": "BuildUrls",
      "name": "Build Download URLs",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [-80, 240]
    },
    {
      "parameters": {
        "url": "={{ $json.url }}",
        "options": { "timeout": 30000 }
      },
      "id": "DownloadCSV",
      "name": "Download CSV from MinIO",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [160, 240]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Recibe 24 items (uno por hora) con el contenido CSV de cada fichero\n// Los une en un único CSV curated\nconst allItems = $input.all();\nconst first = $('Build Download URLs').first().json;\nconst date = first.date;\nconst year = first.year;\nconst month = first.month;\nconst day = first.day;\n\nlet header = '';\nconst dataRows = [];\n\nfor (const item of allItems) {\n  // El HTTP Request devuelve el contenido en item.json.data o como texto\n  let csvText = '';\n  if (typeof item.json === 'string') {\n    csvText = item.json;\n  } else if (item.json.data) {\n    csvText = item.json.data;\n  } else {\n    // Intentar convertir el objeto a CSV\n    csvText = JSON.stringify(item.json);\n  }\n\n  const lines = csvText.trim().split('\\n');\n  if (lines.length < 2) continue;\n  if (!header) header = lines[0];\n  for (let i = 1; i < lines.length; i++) {\n    if (lines[i].trim()) dataRows.push(lines[i]);\n  }\n}\n\n// Ordena por hora (columna hour, posicion 6)\ndataRows.sort(function(a, b) {\n  const ha = a.split(',')[6] || '00';\n  const hb = b.split(',')[6] || '00';\n  return ha.localeCompare(hb);\n});\n\nconst curatedCsv = header + '\\n' + dataRows.join('\\n');\nconst curatedFileName = year + '_' + month + '_' + day + '.csv';\nconst curatedPath = 'curated/daily_summary/' + year + '/' + month + '/' + day + '/' + curatedFileName;\n\nreturn [{\n  json: { date: date, year: year, month: month, day: day, curatedPath: curatedPath, curatedFileName: curatedFileName, totalRows: dataRows.length },\n  binary: {\n    data: {\n      data: Buffer.from(curatedCsv).toString('base64'),\n      mimeType: 'text/csv',\n      fileName: curatedFileName\n    }\n  }\n}];"
      },
      "id": "MergeAndBuild",
      "name": "Merge and Build Curated",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [400, 240]
    },
    {
      "parameters": {
        "operation": "upload",
        "bucketName": "madrid-openmeteo-environment",
        "fileName": "={{ $json.curatedPath }}",
        "additionalFields": {}
      },
      "id": "UploadCurated",
      "name": "Upload Curated to MinIO",
      "type": "n8n-nodes-base.s3",
      "typeVersion": 1,
      "position": [640, 240],
      "credentials": { "s3": { "id": "x6prTWZ53pyVMkQS", "name": "S3 account" } }
    }
  ],
  "pinData": {},
  "connections": {
    "Schedule Trigger": {
      "main": [[{ "node": "Build Download URLs", "type": "main", "index": 0 }]]
    },
    "When clicking 'Execute workflow'": {
      "main": [[{ "node": "Build Download URLs", "type": "main", "index": 0 }]]
    },
    "Build Download URLs": {
      "main": [[{ "node": "Download CSV from MinIO", "type": "main", "index": 0 }]]
    },
    "Download CSV from MinIO": {
      "main": [[{ "node": "Merge and Build Curated", "type": "main", "index": 0 }]]
    },
    "Merge and Build Curated": {
      "main": [[{ "node": "Upload Curated to MinIO", "type": "main", "index": 0 }]]
    }
  },
  "active": false,
  "settings": { "executionOrder": "v1", "binaryMode": "separate" },
  "versionId": "00c2e34b-ad2f-42ad-925c-960eb061885b",
  "meta": {
    "templateCredsSetupCompleted": true,
    "instanceId": "f0a5fd3a6c279fbb2c697c8c0d30357f09b0d7fbb268bee6dc0ca478c07edb11"
  },
  "id": "madrid_analitica",
  "tags": []
}
